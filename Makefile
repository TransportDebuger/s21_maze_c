CC = gcc
CFLAGS = -Wall -Werror -Wextra -x=c -std=c11 -pedantic

PROJECT_NAME = "S21_MAZE"

PHONY.: all install uninstall build tests clean linter

all:

install:

uninstall:

build:

clean:

linter:

tests:


